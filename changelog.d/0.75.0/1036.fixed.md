- **SFT gradient-checkpointing tiers now make the distinct trade-offs they print (#796 by @Amix29 in #1036).** `medium` reaches Transformers as `every_n_layers=2` instead of checkpointing every block, while `selective` wraps only each decoder block's direct attention child and leaves HuggingFace full-model checkpointing off, including after PEFT enables it for a k-bit model. Architectures without a selectable attention child report and apply a full fallback instead of claiming an inactive tier. A four-layer tiny Llama now measures 4/4 checkpointed blocks under `full`, 2/4 under `medium`, and four attention-only wrappers under `selective`, with a real CPU forward/backward completing in the selective arm.
