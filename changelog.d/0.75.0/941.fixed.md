- **`soup reward stress verifiable --verifiable-domain json_schema` now forwards supplied
  references as `schema` metadata instead of reporting that `--references` is missing (#937 in
  #941).** JSON-object schema fields are decoded before scoring, and a verifier that returns the
  wrong number of scores after receiving references now gets a distinct contract diagnostic.
