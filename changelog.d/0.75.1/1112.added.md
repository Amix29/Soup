- **Llasa can now encode raw audio with Transformers-native XCodec2 during TTS training (#265 by @Amix29 in #1112).**
  The live path resamples to 16 kHz, emits Llasa speech tokens, and keeps codec
  imports lazy. The same change refuses Sesame CSM on the text-SFT codec-string
  path because CSM's 32 parallel Mimi codebooks require a dedicated multimodal
  trainer rather than a text assistant turn.
