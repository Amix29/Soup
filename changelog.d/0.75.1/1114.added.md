- **Adapter manifests can now be signed and verified with Sigstore keyless OIDC (#185 by @Amix29 in #1114).**
  `soup adapters sign --backend sigstore` records the Fulcio/Rekor bundle, and
  verification requires an out-of-band certificate identity and OIDC issuer
  instead of trusting the identity carried by the bundle itself.
